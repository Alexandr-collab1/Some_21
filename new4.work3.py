def login(usser_name, password):
    assert usser_name == "Muhailo", "No having"
    assert password == 8945, "Not password"

a = int(input("password >: "))
b = str(input("login >: "))
print(a)
login(b, a)
